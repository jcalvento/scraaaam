mongo:
  image: mongo
  ports:
    - "27017:27017"
  command: "--smallfiles --logpath=/dev/null"
backend:
  build: .
  ports:
    - "3001:3001"
  links:
    - mongo
  environment: 
    DATABASE_URL: 'mongodb://mongo/scram'